package com.example.orderservice.service;

// Commented out - Promotion service not implemented yet
/*
 * import org.springframework.cloud.openfeign.FeignClient;
 * import org.springframework.web.bind.annotation.PostMapping;
 * import org.springframework.web.bind.annotation.RequestParam;
 * 
 * import java.math.BigDecimal;
 * 
 * @FeignClient(name = "promotion-service", url =
 * "${promotion.service.url:http://localhost:8082}")
 * public interface PromotionServiceClient {
 * 
 * @PostMapping("/promotions/apply")
 * BigDecimal applyPromotion(@RequestParam("userId") Long userId,
 * 
 * @RequestParam("itemId") Long itemId,
 * 
 * @RequestParam("quantity") Integer quantity);
 * }
 */
